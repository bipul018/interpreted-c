const std = @import("std");
const vm = @import("engine.zig");
const gen = @import("gencode.zig");

pub fn main() !void{

    //Make a s expression parser consisting of defun, let, setf, +, -, *, /
    // and generates code directly if needed

    

}



test {
    _=@import("engine.zig");
    _=@import("gencode.zig");
}
